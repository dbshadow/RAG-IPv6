## 1. CSS Theme Variables and Color System

- [x] 1.1 Refactor `app/static/style.css` to define `[data-theme="dark"]` (dark gray palette) and `[data-theme="light"]` (white background and light palette) variables
- [x] 1.2 Adapt chat bubbles, preset cards, markdown code blocks, and citation drawer styles to work seamlessly in both modes

## 2. Frontend Theme Toggle UI and Interaction

- [x] 2.1 Add Dark / Light segmented toggle buttons in `app/static/index.html` sidebar
- [x] 2.2 Implement theme switching and `localStorage` persistence logic in `app/static/app.js`

## 3. Verification

- [x] 3.1 Verify UI appearance and contrast in Dark (dark gray) and Light (white) modes
- [x] 3.2 Verify persistence across page reloads and smooth theme transitions
